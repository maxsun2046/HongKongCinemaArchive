export default function MyCollectionStats({ films }) { const stats = { owned: films.filter(f => f.collection.cloud.status === 'available' || f.collection.nas.exists || f.collection.physical.type).length, cloud: films.filter(f => f.collection.cloud.status === 'available').length, nas: films.filter(f => f.collection.nas.exists).length, physical: films.filter(f => f.collection.physical.type).length, watched: films.filter(f => f.watchHistory?.length).length }; return <section className="collection-overview"><div className="section-heading"><p>我的电影收藏</p><h2>资源概览</h2></div><div className="collection-stat-grid">{[['拥有资源',stats.owned],['云端收藏',stats.cloud],['NAS收藏',stats.nas],['实体收藏',stats.physical],['已观看',stats.watched]].map(([label,value])=><div key={label}><small>{label}</small><strong>{value}</strong></div>)}</div></section> }
